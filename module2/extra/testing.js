function getGreeting(name) {
    return 'Hello ' + name + '!';
    }
  
    const getGreeting = (name) => `hello ${name}!`;
    console.log(getGreeting('John'));


    function sum(a, b) {
        return a + b;
    }