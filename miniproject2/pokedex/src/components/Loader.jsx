import React from 'react';

const Loader = () => {
    return (
        <div className='loader'>
            <div></div>
        </div>
    );
};

export default Loader;

//The Loader component is a simple visual placeholder that displays a loading spinner by rendering a div with a loader class, which can be styled with CSS to indicate loading states in the application.